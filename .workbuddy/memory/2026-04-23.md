# 2026-04-23 工作日志

### postbuild 自动复制脚本
- 新增 `build/scripts/copy-dist.mjs`：vite build 完成后自动将产物复制到网站根目录
  - `dist/index.html` → `../index.html`
  - `dist/assets/*`  → `../assets/`（先清空旧 hash 文件再复制）
- `build/package.json` 加了 `"postbuild": "node scripts/copy-dist.mjs"` 钩子（npm 自动在 build 后触发）
- 同步加了 `"type": "module"` 消除 Node ESM 警告
- 构建通过 ✓，无 warning ✓

### README 和 LICENSE
- 新增 `README.md`（中英双语）：前部介绍产品（功能特性、目录结构、快速开始、构建部署），后部介绍开发方式（开发环境、开发流程、注意事项）
- 新增 `LICENSE`（MIT）：标准 MIT 协议文本，年份 2024-2026

### 中英双语支持
- `build/src/main.ts` 重构：顶部加入 `zh`/`en` 翻译字典（共 ~100 个 key，覆盖全站所有文本）
- `data-i18n` 属性标识可翻译节点，`renderI18n()` 动态替换文本
- `setLang()` 切换语言 + 写 localStorage；`getLang()` 读取偏好（默认中文）
- 导航栏右上角新增「EN / 中」切换按钮（`lang-toggle-btn`）
- 所有页面区块均已翻译：Hero、优势、技术、功能卡片、FAQ、快速上手、技术栈详情、更新日志、下载区、页脚、移动菜单
- FAQ 区块用 `data-i18n-html` 支持含 `<strong>` 标签的富文本
- 构建通过 ✓，postbuild 正常 ✓
